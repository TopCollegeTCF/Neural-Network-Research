/* ===============================================
  ФАЙЛ: config.js
  НАЗНАЧЕНИЕ: Хранение всех конфигурационных параметров игры
  ОСОБЕННОСТЬ: Не зависит от других сккриптов
  ================================================ */

/**
 * ОБЪЕКТ КОНФИГУРАЦИИ
 * Содержит все настройки игры в одном месте для удобноо изменения
 */
const CONFIG = {
  /* ===== РАЗМЕРЫ КАНВАСА ===== */
  canvasWight: 800, // Ширина игрового поля в пикелях
  canvasHeight: 600, // Высота игроого поля в пикселях
  
  /* ===== РАЗМЕРЫ КАНВАСА ===== */
  playerSize: 40,    //Размер игока в пикселях (квадрат 40x40)
  playerColor: '#44aaff', // Цвет игрока (голубой, космический)
  playerSpeed: 5,    // Скорость перемещения в пикселях за кадр

  /* ===== ГРАНИЦЫ ДВИЖЕНИЯ ===== */
  /**
   * Границы рассчитываются с учётом размера игрока
   * xMax = canvasWidth - playerSize (800 - 40 = 760)
   * yMax = canvasHeight - playerSize (600 - 40 = 560)
   * Это гарантирует, что игрок не выйдет за пределы видимой области
   */
  
  bounds: {
    xMin: 0,      // Левая граница (игрок не может уйти левее 0)
    xMax: 760,    // Правая граница (800 - 40)
    yMin: 0,      // Верхняя граница
    yMax: 560     // Нижняя граница (600 - 40)
  },
  
  /* ===== ВИЗУАЛЬНЫЕ НАСТРОЙКИ ===== */
  
  backgroundColor: '#050b1a',  // Цвет фона (тёмно-синий, космос)
  starColor: '#ffffff'         // Цвет звёзд (белый)

};

// ====================== ЭКСПОРТ ОБЪЕКТА ======================

/**
   * Делаем CONFIG глобально доступным через window
   */
  
window.CONFIG = CONFIG;
